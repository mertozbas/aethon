__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.pytest_cache/
.coverage
.ruff_cache/
.mypy_cache/
.venv/
venv/
env/
.env
*.so
.DS_Store

# Editor backup sidecars (strands editor; disabled via EDITOR_DISABLE_BACKUP, belt+braces)
*.bak
.idea/
.vscode/
repl_state/
workspace/
Users/

# Local development notes/plans — never pushed
_dev/

# Development-process docs — design docs, checklists, SDK references we use while
# building AETHON. Local-only (like _dev/); the published docs/ describe AETHON,
# not how it was built. Kept on disk, never pushed.
docs/development/
docs/checklists/
docs/references/

# Claude Code workspace config — local dev guidance, never published.
# Root-anchored so it doesn't touch codex-proxy's own tracked .claude/.
/CLAUDE.md
/.claude/

# Vendored codex-proxy — never commit its secrets / runtime state / deps
codex-proxy/node_modules/
codex-proxy/**/node_modules/
codex-proxy/data/
codex-proxy/logs/
codex-proxy/.env
codex-proxy/**/dist/
