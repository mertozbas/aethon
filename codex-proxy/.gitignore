node_modules/
dist/
dist-electron/
release/
public-desktop/
data/
# Ship the built developer dashboard so a fresh clone serves the UI without a
# `npm run build:web` step (the /v1 API works without it, but the dashboard 404s).
# Everything else generated under public/ stays ignored.
public/*
!public/index.html
!public/assets/
!public/assets/**
docs/
bin/*
!bin/README.md
.env
*.log
.asar-out/
tmp/
.claude/*
!.claude/skills/
native/target/
native/codex-tls.linux-*.node

# Maintenance scripts (private)
scripts/manual-test/
scripts/cron-update.sh

# Design / internal files
stitch-designs/
stitch-screens/
CLAUDE.md
*.tar.gz
stitch-output/
